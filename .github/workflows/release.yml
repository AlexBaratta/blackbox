name: Build and Release

on:
  push:
    branches: [main]
    tags:
      - 'v*'
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Build
        run: pyinstaller --noconfirm --clean Blackbox.spec

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Blackbox-windows
          path: dist/Blackbox.exe

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pyinstaller

      - name: Create macOS icon from PNG (if icns doesn't exist)
        run: |
          if [ -f "assets/blackbox_logo.icns" ]; then
            echo "icon.icns already exists"
          elif [ -f "assets/blackbox_logo.png" ]; then
            echo "Generating icon.icns from icon.png"
            mkdir -p icon.iconset
            sips -z 16 16     assets/blackbox_logo.png --out icon.iconset/icon_16x16.png
            sips -z 32 32     assets/blackbox_logo.png --out icon.iconset/icon_16x16@2x.png
            sips -z 32 32     assets/blackbox_logo.png --out icon.iconset/icon_32x32.png
            sips -z 64 64     assets/blackbox_logo.png --out icon.iconset/icon_32x32@2x.png
            sips -z 128 128   assets/blackbox_logo.png --out icon.iconset/icon_128x128.png
            sips -z 256 256   assets/blackbox_logo.png --out icon.iconset/icon_128x128@2x.png
            sips -z 256 256   assets/blackbox_logo.png --out icon.iconset/icon_256x256.png
            sips -z 512 512   assets/blackbox_logo.png --out icon.iconset/icon_256x256@2x.png
            sips -z 512 512   assets/blackbox_logo.png --out icon.iconset/icon_512x512.png
            sips -z 1024 1024 assets/blackbox_logo.png --out icon.iconset/icon_512x512@2x.png
            iconutil -c icns icon.iconset -o assets/blackbox_logo.icns
            rm -rf icon.iconset
          fi

      - name: Build
        run: |
          ICON_FLAG=""
          if [ -f "assets/blackbox_logo.icns" ]; then
            ICON_FLAG="--icon assets/blackbox_logo.icns"
          fi
          pyinstaller --noconfirm --clean --onefile --windowed \
            --name Blackbox \
            --paths src \
            --collect-submodules blackbox \
            --add-data "assets:assets" \
            $ICON_FLAG \
            run_blackbox.py

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: Blackbox-mac
          path: dist/Blackbox

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [build-windows, build-mac]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: |
            artifacts/Blackbox-windows/Blackbox.exe
            artifacts/Blackbox-mac/Blackbox
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
